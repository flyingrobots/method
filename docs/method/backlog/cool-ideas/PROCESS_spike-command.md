---
title: "Spike Command"
legend: PROCESS
lane: cool-ideas
---

# Spike Command

Add a `method spike` command that creates a properly prefixed spike
backlog item or packet with the right scaffolding and expectations. The
behavior-spike convention already exists in doctrine, but the repo still
relies on operators to remember the naming and capture rules by hand.
