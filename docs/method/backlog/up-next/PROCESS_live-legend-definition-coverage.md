---
title: "Live Legend Definition Coverage"
legend: PROCESS
lane: up-next
---

# Live Legend Definition Coverage

The backlog is already using `MCP` as a live legend, but
`docs/method/legends/` still only defines `PROCESS` and `SYNTH`. Tests
and signposts currently treat legend docs as the discovered taxonomy,
which means the repo can have real legend-bearing work that is not
actually documented as a first-class legend.

This is taxonomy drift. If a legend is real enough to organize backlog
work, it should either have a legend doc or stop being used as a legend.
